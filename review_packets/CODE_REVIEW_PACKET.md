# Code Review Packet

The previous flat five-file packet is superseded by the mandatory bounded
packets in [`review_packets/code_packets/`](code_packets/README.md).

The new packet is anchored to sprint baseline commit
`1baaadf313f4d8a91018321db1317c5c6b385ccc`, includes only files changed after
that baseline, supplies all required reviewer metadata, and limits every
implementation area to three critical files.
